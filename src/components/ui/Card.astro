---
import {
  card,
  cardImage,
  cardImageInner,
  cardDateOverlay,
  cardDateLabel,
  cardDateIcon,
  cardContent,
  cardTitle,
  cardDescription,
  cardHead,
  cardHeadItem,
  cardMeta,
  cardAuthor,
  cardAuthorName,
  cardSource,
  cardTagList,
  cardTag,
} from '../../styles/components/card.css';

interface Props {
  title: string;
  description?: string;
  href: string;
  date?: Date;
  author?: string;
  tags?: string[];
  source?: string;
  external?: boolean;
  image?: string;
  imageAlt?: string;
  head?: string[];
}

const {
  title,
  description,
  href,
  date,
  author,
  tags,
  source,
  external,
  image,
  imageAlt,
  head,
} = Astro.props;

const displayHead = head?.slice(0, 2) ?? [];

const formatDate = (d: Date) =>
  d.toLocaleDateString('ja-JP', {
    year: 'numeric',
    month: 'short',
    day: 'numeric',
  });
---

<a
  href={href}
  target={external ? '_blank' : undefined}
  rel={external ? 'noopener noreferrer' : undefined}
  class={card}
>
  {
    image && (
      <div class={cardImage}>
        <img src={image} alt={imageAlt ?? title} class={cardImageInner} loading="lazy" />
        {
          date && (
            <div class={cardDateOverlay}>
              <span class={cardDateIcon} aria-hidden="true">📅</span>
              <span class={cardDateLabel}>公開</span>
              <time datetime={date.toISOString()}>{formatDate(date)}</time>
            </div>
          )
        }
      </div>
    )
  }
  <div class={cardContent}>
    <h3 class={cardTitle}>{title}</h3>
    {
      displayHead.length > 0 && (
        <ul class={cardHead}>
          {displayHead.map((line) => (
            <li class={cardHeadItem}>{line}</li>
          ))}
        </ul>
      )
    }
    {description && !image && <p class={cardDescription}>{description}</p>}
    {
      (author || source) && (
        <div class={cardMeta}>
          {
            author && (
              <span class={cardAuthor}>
                <span class={cardAuthorName}>@{author}</span>
              </span>
            )
          }
          {
            source && (
              <span class={cardSource}>
                {source}
                {external ? ' ↗' : ''}
              </span>
            )
          }
        </div>
      )
    }
    {
      tags && tags.length > 0 && (
        <div class={cardTagList}>
          {tags.map((tag) => (
            <span class={cardTag}>{tag}</span>
          ))}
        </div>
      )
    }
  </div>
</a>
